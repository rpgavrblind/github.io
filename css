body, html {
    margin: 0;
    padding: 0;
    height: 100%;
    overflow: hidden;
}

.full-screen-button {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0); /* Torna o botão transparente */
    border: none;
    cursor: pointer;
    z-index: 9999; /* Coloca o botão na parte superior */
}
